<div class="banner">
  <div class="container">
    <h1><%= @list.name %></h1>
  </div>
</div>
<div class="container mt-4">
  <div class="card">
    <p class="card-title"><%= @list.name %></p>

    <%= link_to new_list_bookmark_path(@list), class: "bookmark" do %>
      <i class="fas fa-plus-circle"></i>
    <% end %>
  </div>

  <%= link_to lists_path, class: "btn-accueil" do %>
    <i class="fas fa-home"></i>
  <% end %>
</div>
